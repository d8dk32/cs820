To run tests:

execute 'make myTests', which will compile testHarness.c with alloc.c and asmAlloc.s, producin an executable called 'test'.

This executable takes 1 parameter, a number from 1 to 12 (as in ./test 1). This will execute one of the 12 test cases.